{
  "name": "shadab-phuphi-game",
  "version": "1.0.0",
  "private": true,
  "description": "Shadab Phuphi Birthday Challenge - real-time multiplayer birthday game (React + Express + Socket.IO, no database)",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build": "npm install --include=dev --prefix client && npm run build --prefix client && npm install --omit=dev --prefix server",
    "start": "node server/server.js",
    "test": "npm test --prefix server"
  },
  "engines": {
    "node": ">=18"
  }
}
